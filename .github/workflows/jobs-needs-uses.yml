name: jobs-needs-uses

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     - name: checkout repo
       uses: actions/checkout@v4

     - name: Run a one-line script
       run: echo "Hello, world!"

  test:
     runs-on: ubuntu-latest

     steps:
        - name: test-job
          run: echo "testing"


